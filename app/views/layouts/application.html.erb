<!DOCTYPE html>
<html>
  <head>
    <title>Ryan' Adopt Don't Shop</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <% flash.each do |type, message| %>
      <p><%= message %></p>
  <% end %>

  <body>
  <div class="navbar">
    <!-- Session index below will fail tests, runs on local-->
    <% session[:index].each do |object| %>
      <%= object %>
    <% end %>
    <!-- THIS IS TO SERVE AS A REMINDER TO FIX TESTS OR GET ME OUT-->
    <h1 id="main-title">Adopt Don't Shop</h1>
    <div class="nav-links">
      <%= link_to "Pets Index", "/pets" %>
      |
      <%= link_to "Shelters Index", "/shelters" %>
    </div>
  </div>
    <%= yield %>
  </body>
</html>
