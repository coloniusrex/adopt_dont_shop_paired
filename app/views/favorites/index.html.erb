<section class="list">
  <% @favorite_pets.each do |pet| %>
    <section id="favorite-<%= pet.id %>">
      <%= link_to pet.name, "/pets/#{pet.id}" %>
      <%= image_tag pet.image_url %>
      <%= link_to "Un-Favorite Pet", "/favorites/#{pet.id}", method: :delete %>
    </section>
  <% end %>
</section>
