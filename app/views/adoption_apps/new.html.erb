<h1>Pets Adoption Application</h1>
<%= form_tag "/adoption_apps/create", method: :post do%>
  <section class="field-container">
    <section class="favorites-selection">
      <% @favorite_pets.each do |pet| %>
        <%= check_box_tag pet.name, pet.id %>
        <%= label_tag pet.name, pet.name %>
      <% end %>
    </section>
    <section class="adoption-info">
      
    </section>
  </section>
<% end %>
